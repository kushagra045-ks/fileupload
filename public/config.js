// Point this at wherever you deployed server.js (Render, Railway, Fly.io, a VPS...).
// Leave it as an empty string only if the frontend and backend are served from
// the exact same origin (e.g. both on the same Node server, no Cloudflare Pages split).
//
// Example: window.API_BASE = "https://wavelength-backend.onrender.com";
window.API_BASE = "";
